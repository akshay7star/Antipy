import { Category } from '../types';

export const setMethods: Category = {
    id: 'set-methods',
    name: 'Set Methods',
    description: 'All methods for Python sets — mutable unordered collections of unique elements',
    icon: 'CircleDot',
    entries: [
        { id: 'set-add', name: 'set.add()', signature: 'set.add(elem) -> None', description: 'Adds an element to the set. Does nothing if already present.', example: 's = {1, 2, 3}\ns.add(4)\nprint(s)  # {1, 2, 3, 4}\ns.add(2)  # No effect\nprint(s)  # {1, 2, 3, 4}', complexity: 'O(1) average', tags: ['add', 'insert'] },
        { id: 'set-remove', name: 'set.remove()', signature: 'set.remove(elem) -> None', description: 'Removes an element. Raises KeyError if not present.', example: 's = {1, 2, 3}\ns.remove(2)\nprint(s)  # {1, 3}\n# s.remove(99)  # KeyError!', edgeCases: ['Use discard() if you don\'t want KeyError on missing elements.'], complexity: 'O(1) average', tags: ['remove', 'delete', 'error'] },
        { id: 'set-pop', name: 'set.pop()', signature: 'set.pop() -> elem', description: 'Removes and returns an arbitrary element. Raises KeyError if set is empty.', example: 's = {1, 2, 3}\nelem = s.pop()\nprint(elem)  # Some element (order is arbitrary)', edgeCases: ['Element removed is arbitrary (sets are unordered).', 'Raises KeyError on empty set.'], complexity: 'O(1)', tags: ['remove', 'return', 'random'] },

        { id: 'set-clear', name: 'set.clear()', signature: 'set.clear() -> None', description: 'Removes all elements from the set.', example: 's = {1, 2, 3}\ns.clear()\nprint(s)  # set()', tags: ['remove', 'empty', 'reset'] },
        { id: 'set-copy', name: 'set.copy()', signature: 'set.copy() -> set', description: 'Returns a shallow copy of the set.', example: 'a = {1, 2, 3}\nb = a.copy()\nb.add(4)\nprint(a)  # {1, 2, 3}\nprint(b)  # {1, 2, 3, 4}', tags: ['copy', 'clone'] },
        { id: 'set-union', name: 'set.union()', signature: 'set.union(*others) -> set | set | set', description: 'Returns all elements that are in either set. Equivalent to | operator.', example: 'a = {1, 2, 3}\nb = {3, 4, 5}\nprint(a.union(b))       # {1, 2, 3, 4, 5}\nprint(a | b)            # {1, 2, 3, 4, 5}', complexity: 'O(len(a) + len(b))', tags: ['combine', 'merge', 'or'] },
        { id: 'set-intersection', name: 'set.intersection()', signature: 'set.intersection(*others) -> set | set & set', description: 'Returns elements common to all sets. Equivalent to & operator.', example: 'a = {1, 2, 3, 4}\nb = {3, 4, 5, 6}\nprint(a.intersection(b))  # {3, 4}\nprint(a & b)              # {3, 4}', complexity: 'O(min(len(a), len(b)))', tags: ['common', 'overlap', 'and'] },
        { id: 'set-difference', name: 'set.difference()', signature: 'set.difference(*others) -> set | set - set', description: 'Returns elements in the set but not in others. Equivalent to - operator.', example: 'a = {1, 2, 3, 4}\nb = {3, 4, 5}\nprint(a.difference(b))  # {1, 2}\nprint(a - b)            # {1, 2}', tags: ['subtract', 'exclude', 'minus'] },
        { id: 'set-symmetric-difference', name: 'set.symmetric_difference()', signature: 'set.symmetric_difference(other) -> set | set ^ set', description: 'Returns elements in either set but not in both. Equivalent to ^ operator.', example: 'a = {1, 2, 3}\nb = {2, 3, 4}\nprint(a.symmetric_difference(b))  # {1, 4}\nprint(a ^ b)                       # {1, 4}', tags: ['xor', 'exclusive', 'difference'] },
        { id: 'set-update', name: 'set.update()', signature: 'set.update(*others) -> None | set |= set', description: 'Adds all elements from other sets (union in place).', example: 'a = {1, 2}\na.update({3, 4}, {5})\nprint(a)  # {1, 2, 3, 4, 5}\n\na |= {6, 7}\nprint(a)  # {1, 2, 3, 4, 5, 6, 7}', tags: ['merge', 'update', 'inplace'] },
        { id: 'set-intersection-update', name: 'set.intersection_update()', signature: 'set.intersection_update(*others) -> None | set &= set', description: 'Keeps only elements found in all sets (intersection in place).', example: 'a = {1, 2, 3, 4}\na.intersection_update({2, 3, 5})\nprint(a)  # {2, 3}', tags: ['filter', 'intersect', 'inplace'] },
        { id: 'set-difference-update', name: 'set.difference_update()', signature: 'set.difference_update(*others) -> None | set -= set', description: 'Removes all elements found in other sets (difference in place).', example: 'a = {1, 2, 3, 4}\na.difference_update({3, 4, 5})\nprint(a)  # {1, 2}', tags: ['remove', 'subtract', 'inplace'] },
        { id: 'set-symmetric-difference-update', name: 'set.symmetric_difference_update()', signature: 'set.symmetric_difference_update(other) -> None | set ^= set', description: 'Keeps only elements found in one set but not both (symmetric difference in place).', example: 'a = {1, 2, 3}\na.symmetric_difference_update({2, 3, 4})\nprint(a)  # {1, 4}', tags: ['xor', 'update', 'inplace'] },
        { id: 'set-issubset', name: 'set.issubset()', signature: 'set.issubset(other) -> bool | set <= set', description: 'Returns True if all elements of the set are in the other set.', example: 'a = {1, 2}\nb = {1, 2, 3, 4}\nprint(a.issubset(b))  # True\nprint(a <= b)         # True\nprint(a < b)          # True (proper subset)', tags: ['check', 'subset', 'contains'] },
        { id: 'set-issuperset', name: 'set.issuperset()', signature: 'set.issuperset(other) -> bool | set >= set', description: 'Returns True if all elements of the other set are in this set.', example: 'a = {1, 2, 3, 4}\nb = {1, 2}\nprint(a.issuperset(b))  # True\nprint(a >= b)           # True', tags: ['check', 'superset', 'contains'] },
        { id: 'set-isdisjoint', name: 'set.isdisjoint()', signature: 'set.isdisjoint(other) -> bool', description: 'Returns True if the two sets have no elements in common.', example: 'a = {1, 2, 3}\nb = {4, 5, 6}\nprint(a.isdisjoint(b))  # True\n\nc = {3, 4}\nprint(a.isdisjoint(c))  # False', tags: ['check', 'overlap', 'disjoint'] },
        { id: 'set-comprehension', name: 'Set Comprehension', signature: '{expression for item in iterable if condition}', description: 'Concise way to create sets with automatic deduplication.', example: 'squares = {x**2 for x in range(-3, 4)}\nprint(squares)  # {0, 1, 4, 9}\n\ntext = "hello world"\nvowels = {c for c in text if c in "aeiou"}\nprint(vowels)  # {\'e\', \'o\'}', tags: ['comprehension', 'create', 'unique'] },
    ]
};
